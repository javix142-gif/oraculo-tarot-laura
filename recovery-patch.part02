; showScreen('history'); });
   elements.homeBrand.addEventListener('click', goHome);
   elements.backButton.addEventListener('click', goBack);
-  elements.aboutButton.addEventListener('click', () => elements.aboutDialog.showModal());
+  elements.aboutButton.addEventListener('click', () => showManagedDialog(elements.aboutDialog, elements.aboutButton, elements.closeAboutButton));
   elements.closeAboutButton.addEventListener('click', () => elements.aboutDialog.close());
   elements.dialogOkButton.addEventListener('click', () => elements.aboutDialog.close());
   elements.aboutDialog.addEventListener('click', closeDialogFromBackdrop);
@@ -59,9 +61,12 @@ function bindEvents() {
   elements.readingForm.addEventListener('submit', startDraw);
   elements.questionInput.addEventListener('input', updateQuestionCount);
   elements.newReadingButton.addEventListener('click', () => showScreen('setup'));
+  elements.shareReadingButton.addEventListener('click', shareCurrentReading);
+  elements.detailShareButton.addEventListener('click', shareCurrentReading);
   elements.resultHomeButton.addEventListener('click', goHome);
   elements.clearHistoryButton.addEventListener('click', confirmClearHistory);
   elements.installButton.addEventListener('click', installApp);
+  [elements.aboutDialog, elements.confirmDialog].forEach((dialog) => dialog.addEventListener('close', () => restoreDialogFocus(dialog)));
   window.addEventListener('popstate', handlePopState);
   window.addEventListener('beforeinstallprompt', handleInstallPrompt);
   window.addEventListener('appinstalled', () => {
@@ -81,7 +86,10 @@ function showScreen(screen, { replaceHistory = false, focus = true } = {}) {
     item.hidden = !active;
     item.classList.toggle('active', active);
   });
-  elements.backButton.classList.toggle('hidden', screen === 'home');
+  const isHome = screen === 'home';
+  elements.backButton.classList.toggle('header-slot-empty', isHome);
+  elements.backButton.setAttribute('aria-hidden', String(isHome));
+  elements.backButton.tabIndex = isHome ? -1 : 0;
   const historyState = { screen };
   if (replaceHistory) window.history.replaceState(historyState, '', `#${screen}`);
   else if (window.history.state?.screen !== screen) window.history.pushState(historyState, '', `#${screen}`);
@@ -258,6 +266,7 @@ function renderHistory() {
 }
 
 function openHistoryReading(reading) {
+  state.currentReading = reading;
   renderReading(reading, elements.detailCards, elements.detailDate, elements.detailQuestion);
   showScreen('detail');
 }
@@ -280,12 +289,92 @@ async function confirmClearHistory() {
 
 function requestConfirmation(message) {
   elements.confirmMessage.textContent = message;
-  elements.confirmDialog.showModal();
+  showManagedDialog(elements.confirmDialog, document.activeElement, elements.confirmDialog.querySelector('[value="cancel"]'));
   return new Promise((resolve) => {
     elements.confirmDialog.addEventListener('close', () => resolve(elements.confirmDialog.returnValue === 'confirm'), { once: true });
   });
 }
 
+function showManagedDialog(dialog, trigger = document.activeElement, initialFocus = null) {
+  if (!dialog || dialog.open) return;
+  dialogReturnFocus.set(dialog, trigger instanceof HTMLElement ? trigger : null);
+  dialog.showModal();
+  requestAnimationFrame(() => {
+    const target = initialFocus || dialog.querySelector('button, [href], input, textarea, select, [tabindex]:not([tabindex="-1"])');
+    target?.focus({ preventScroll: true });
+  });
+}
+
+function restoreDialogFocus(dialog) {
+  const target = dialogReturnFocus.get(dialog);
+  dialogReturnFocus.delete(dialog);
+  if (target?.isConnected) requestAnimationFrame(() => target.focus({ preventScroll: true }));
+}
+
+function updateShareButtonLabels() {
+  const label = typeof navigator.share === 'function' ? 'Compartir lectura' : 'Copiar lectura';
+  elements.shareReadingButton.textContent = label;
+  elements.detailShareButton.textContent = label;
+}
+
+async function shareCurrentReading() {
+  const reading = state.currentReading;
+  if (!reading) {
+    showToast('No hay una lectura disponible para compartir.');
+    return;
+  }
+  const text = formatReadingForSharing(reading);
+  if (typeof navigator.share === 'function') {
+    try {
+      await navigator.share({ title: 'Oráculo Tarot Laura', text });
+      return;
+    } catch (error) {
+      if (error?.name === 'AbortError') return;
+    }
+  }
+  try {
+    await copyReadingText(text);
+    showToast('Lectura copiada.');
+  } catch (error) {
+    console.warn('No fue posible copiar la lectura.', error);
+    showToast('No fue posible compartir ni copiar la lectura.');
+  }
+}
+
+function formatReadingForSharing(reading) {
+  const lines = [
+    'Oráculo Tarot Laura — Lectura y Cartas',
+    `${reading.spreadLabel} · ${formatDate(reading.createdAt)}`,
+  ];
+  if (reading.question) lines.push(`Pregunta: “${reading.question}”`);
+  for (const card of reading.cards) {
+    lines.push('', `${card.position}: ${card.name}`, `Palabras clave: ${card.keywords.join(', ')}`, card.interpretation);
+  }
+  lines.push('', 'Lectura recreativa y de reflexión personal.');
+  return lines.join('\n');
+}
+
+async function copyReadingText(text) {
+  if (navigator.clipboard?.writeText) {
+    try {
+      await navigator.clipboard.writeText(text);
+      return;
+    } catch (error) {
+      console.warn('Clipboard API no disponible; se usará la alternativa compatible.', error);
+    }
+  }
+  const textarea = document.createElement('textarea');
+  textarea.value = text;
+  textarea.setAttribute('readonly', '');
+  textarea.style.position = 'fixed';
+  textarea.style.opacity = '0';
+  document.body.append(textarea);
+  textarea.select();
+  const copied = document.execCommand('copy');
+  textarea.remove();
+  if (!copied) throw new Error('La copia compatible fue rechazada.');
+}
+
 function closeDialogFromBackdrop(event) {
   if (event.target === event.currentTarget) event.currentTarget.close('cancel');
 }
diff --git a/android-web/service-worker.js b/android-web/service-worker.js
index 47cad87..92347a7 100644
--- a/android-web/service-worker.js
+++ b/android-web/service-worker.js
@@ -1,4 +1,4 @@
-const CACHE_NAME = 'oraculo-tarot-laura-v1';
+const CACHE_NAME = 'oraculo-tarot-laura-v2';
 const CORE_PATHS = [
   "./",
   "./index.html",
diff --git a/android-web/styles.css b/android-web/styles.css
index 421dc43..9f17a4e 100644
--- a/android-web/styles.css
+++ b/android-web/styles.css
@@ -14,12 +14,17 @@
   --focus: #fff2b7;
   --radius: 22px;
   --shadow: 0 24px 70px rgba(0, 0, 0, 0.34);
+  --safe-area-inset-top: env(safe-area-inset-top, 0px);
+  --safe-area-inset-right: env(safe-area-inset-right, 0px);
+  --safe-area-inset-bottom: env(safe-area-inset-bottom, 0px);
+  --safe-area-inset-left: env(safe-area-inset-left, 0px);
 }
 
 * { box-sizing: border-box; }
-html { min-width: 320px; background: var(--bg); scroll-behavior: smooth; }
+html { width: 100%; max-width: 100%; background: var(--bg); scroll-behavior: smooth; overflow-x: hidden; }
 body {
-  min-width: 320px;
+  width: 100%;
+  max-width: 100%;
   min-height: 100vh;
   margin: 0;
   overflow-x: hidden;
@@ -57,20 +62,29 @@ button:disabled { cursor: not-allowed; opacity: .5; }
   background: var(--gold); color: #16102c; border-radius: 10px; font-weight: 700;
 }
 .skip-link:focus { top: 12px; }
-.app-shell { width: min(100%, 920px); min-height: 100vh; margin: 0 auto; padding: env(safe-area-inset-top) 16px env(safe-area-inset-bottom); }
+.app-shell {
+  width: min(100%, 920px);
+  min-height: 100vh;
+  min-height: 100dvh;
+  margin: 0 auto;
+  padding: 0 calc(16px + var(--safe-area-inset-right)) 0 calc(16px + var(--safe-area-inset-left));
+  overflow-x: clip;
+}
 .topbar {
-  position: sticky; top: 0; z-index: 20; display: grid; grid-template-columns: 48px 1fr 48px;
-  align-items: center; min-height: 72px; padding: 8px 0;
+  position: sticky; top: 0; z-index: 20; display: grid; grid-template-columns: 56px minmax(0, 1fr) 56px;
+  align-items: center; min-width: 0; min-height: calc(72px + var(--safe-area-inset-top)); padding: calc(8px + var(--safe-area-inset-top)) 0 8px;
   background: linear-gradient(to bottom, rgba(9,7,27,.97) 58%, transparent);
 }
-.brand-button { display: inline-flex; align-items: center; justify-content: center; gap: 10px; min-height: 48px; padding: 6px; border: 0; background: transparent; cursor: pointer; }
-.brand-button strong, .brand-button small { display: block; }
+.brand-button { display: inline-flex; align-items: center; justify-content: center; gap: 8px; width: 100%; min-width: 0; min-height: 48px; padding: 6px; border: 0; background: transparent; cursor: pointer; overflow: hidden; }
+.brand-button > span:last-child { min-width: 0; overflow: hidden; }
+.brand-button strong, .brand-button small { display: block; min-width: 0; overflow: hidden; white-space: nowrap; text-overflow: ellipsis; }
 .brand-button strong { color: var(--gold-bright); font-size: .95rem; letter-spacing: .025em; }
 .brand-button small { margin-top: 2px; color: var(--muted); font-family: system-ui, sans-serif; font-size: .72rem; }
 .brand-mark { color: var(--gold); font-size: 1.2rem; }
 .icon-button { width: 48px; height: 48px; border: 1px solid rgba(228,193,111,.25); border-radius: 50%; background: rgba(27,18,62,.75); cursor: pointer; font-family: system-ui,sans-serif; font-size: 1.2rem; }
-main { min-height: calc(100vh - 150px); }
-.screen { animation: screen-in .28s ease both; }
+.header-slot-empty { visibility: hidden; pointer-events: none; }
+main { min-width: 0; min-height: calc(100vh - 150px); min-height: calc(100dvh - 150px); padding-bottom: calc(32px + var(--safe-area-inset-bottom)); }
+.screen { min-width: 0; animation: screen-in .28s ease both; }
 .hero-card {
   position: relative; overflow: hidden; margin: 7vh auto 20px; padding: 36px 22px 28px;
   text-align: center; border: 1px solid rgba(228,193,111,.34); border-radius: 30px;
@@ -88,12 +102,13 @@ main { min-height: calc(100vh - 150px); }
 .oracle-emblem i { position: absolute; width: 6px; height: 6px; left: 24px; bottom: 28px; background: var(--gold); border-radius: 50%; }
 h1, h2, h3, p { margin-top: 0; }
 h1 { margin-bottom: 4px; color: var(--gold-bright); font-size: clamp(2rem, 10vw, 3.5rem); line-height: 1.02; }
+#home-title { white-space: nowrap; font-size: clamp(1.6rem, 8.5vw, 3.5rem); }
 h2 { margin-bottom: 10px; color: var(--gold-bright); font-size: clamp(1.65rem, 7vw, 2.45rem); line-height: 1.1; }
 h3 { color: var(--gold-bright); }
 .hero-subtitle { margin-bottom: 18px; color: var(--lavender); font-family: system-ui,sans-serif; font-size: 1rem; letter-spacing: .2em; text-transform: uppercase; }
 .hero-copy, .section-heading p, .reflection-note { color: var(--muted); font-family: system-ui,sans-serif; line-height: 1.6; }
 .primary-actions, .result-actions { display: grid; gap: 12px; margin-top: 26px; }
-.button { min-height: 50px; padding: 13px 18px; border-radius: 15px; border: 1px solid transparent; cursor: pointer; font-family: system-ui,sans-serif; font-weight: 750; transition: transform .18s ease, border-color .18s ease, background .18s ease; }
+.button { min-width: 48px; min-height: 50px; padding: 13px 18px; border-radius: 15px; border: 1px solid transparent; cursor: pointer; font-family: system-ui,sans-serif; font-weight: 750; transition: transform .18s ease, border-color .18s ease, background .18s ease; }
 .button:not(:disabled):active { transform: translateY(1px) scale(.99); }
 .button-primary { color: #1a102b; background: linear-gradient(135deg, var(--gold-bright), #c99e43); box-shadow: 0 9px 26px rgba(228,193,111,.16); }
 .button-secondary { border-color: rgba(228,193,111,.38); background: rgba(42,29,85,.75); }
@@ -134,7 +149,8 @@ textarea::placeholder { color: #8d86a2; }
 .deck-card.selected { animation: selected-rise .45s ease both; }
 .selection-progress { min-height: 28px; margin-bottom: 12px; color: var(--gold); font-family: system-ui,sans-serif; text-align: center; }
 .result-cards { display: grid; gap: 18px; }
-.result-card { display: grid; grid-template-columns: minmax(112px, 34%) 1fr; gap: 16px; padding: 16px; border: 1px solid rgba(228,193,111,.25); border-radius: 20px; background: var(--panel); box-shadow: 0 14px 40px rgba(0,0,0,.2); }
+.result-card { min-width: 0; display: grid; grid-template-columns: minmax(112px, 34%) 1fr; gap: 16px; padding: 16px; border: 1px solid rgba(228,193,111,.25); border-radius: 20px; background: var(--panel); box-shadow: 0 14px 40px rgba(0,0,0,.2); }
+.result-card > div { min-width: 0; }
 .result-card-image { width: 100%; max-width: 220px; margin: 0 auto; border-radius: 13px; border: 1px solid rgba(228,193,111,.42); box-shadow: 0 9px 26px rgba(0,0,0,.25); }
 .position-label { display: inline-block; margin-bottom: 7px; color: var(--gold); font-family: system-ui,sans-serif; font-size: .7rem; font-weight: 800; letter-spacing: .12em; text-transform: uppercase; }
 .result-card h3 { margin-bottom: 8px; font-size: 1.35rem; }
@@ -143,9 +159,10 @@ textarea::placeholder { color: #8d86a2; }
 .keyword-list li { padding: 5px 8px; color: var(--lavender); border: 1px solid rgba(197,184,239,.2); border-radius: 99px; background: rgba(7,5,22,.35); font-family: system-ui,sans-serif; font-size: .7rem; }
 .interpretation { margin: 0; color: #e6e1f2; font-family: system-ui,sans-serif; font-size: .9rem; line-height: 1.58; }
 .reflection-note { margin: 20px 0 0; padding: 15px; text-align: center; border: 1px solid rgba(197,184,239,.12); border-radius: 14px; font-size: .8rem; }
-.history-heading { display: flex; align-items: end; justify-content: space-between; gap: 12px; }
+.history-heading { display: grid; grid-template-columns: minmax(0, 1fr) auto; align-items: end; gap: 12px; min-width: 0; }
+.history-heading > div { min-width: 0; }
 .text-button { min-height: 48px; padding: 8px 4px; border: 0; background: transparent; color: var(--lavender); cursor: pointer; font-family: sys